const projectTitle = document.querySelector(".h1");

projectTitle.textContent = "Judul Baru Yg Di ubah dari javascrip";

// Indentasi:
//     Gunakan indentasi konsisten, biasanya 2 atau 4 spasi per tingkat. Ini membuat struktur kode lebih jelas.
function contoh() {
  if (kondisi) {
    // kode di sini
  } else {
    // kode di sini
  }
}

// Semicolon:
//     Meskipun JavaScript memungkinkan penghilangan semikolon di akhir pernyataan, sebaiknya masukkan semikolon untuk mencegah perilaku yang tidak terduga.
const x = 5;
console.log(x);

// Penempatan Kurung Kurawal:
//     Letakkan kurung kurawal pembuka pada baris yang sama dengan fungsi atau pernyataan kontrol.
function contoh() {
  // kode di sini
}

// Penamaan yang Konsisten:
//     Gunakan nama yang bermakna dan konsisten untuk variabel, fungsi, dan objek. Gunakan konvensi penamaan seperti camelCase.
const variabelSaya = "beberapa nilai";
function hitungJumlah(num1, num2) {
  return num1 + num2;
}

// Spasi:
//     Gunakan spasi yang konsisten seputar operator dan setelah koma.
const hasil = a + b;
const arr = [1, 2, 3];

// Menghindari Penggunaan var:
//     Gunakan const dan let untuk mendeklarasikan variabel. Hindari penggunaan var.
const nilai = 10;
let nama = "John";
