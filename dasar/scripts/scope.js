// scope mrupakan area akses sebuah data
// ada dua jenis local scope yaitu global scope dan local scope

// global scope variabel
let counter = 0;

// global scope hitMe
function hitMe() {
  // local scope hit me
}

// global scope other
function other() {
  // local scope other
}
